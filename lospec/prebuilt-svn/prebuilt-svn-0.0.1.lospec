busybox = assert(requires("prebuilt-busybox"))

package = {
	name = "prebuilt-svn",
	version  = "0.0.1",
	source   = "$(repo.los)/prebuilt-toolchains/prebuilt-svn-"..api.system().."-"..lfs.cpuarch()..".tar.bz2",
	archdir = ""
}

function prepare()
	busybox:install()
	api.download()
	api.unarch()
end

function install()
	prepare()
	api.copy(api.makepathdir(path.src.dir, lfs.cpuarch().."-svn"), path.install.dir)
end
