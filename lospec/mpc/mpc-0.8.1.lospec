
package = {
	name     = "mpc",
	version  = "0.8.1",
	source   = "http://gcc.igor.onlinedirect.bg/infrastructure/mpc-0.8.1.tar.gz"
}

use("autotools")

function install()
	error("not supported yet")
end
